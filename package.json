{
  "name": "crankshaft-spotify-playback-manager",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "format": "prettier src --write",
    "build": "esbuild src/index.tsx --bundle --format=esm --outfile=dist/index.js",
    "build-watch": "esbuild src/index.tsx --bundle --format=esm --outfile=dist/index.js --watch"
  },
  "dependencies": {
    "preact": "^10.10.1"
  },
  "devDependencies": {
    "@crankshaft/types": "^0.2.2",
    "esbuild": "^0.14.51",
    "prettier": "^2.7.1",
    "prettier-plugin-organize-imports": "^3.0.0",
    "typescript": "^4.7.4"
  }
}
